<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="live.cricket.cricketlive">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".activities.MatchesListAct">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".activities.RespectiveData" />
        <activity
            android:name=".trainrelated.LiveTrainStatus"
            android:label="Live Status" />
        <activity
            android:name=".trainrelated.TrainRoute"
            android:label="Train Route" />
        <activity
            android:name=".trainrelated.TrainBWNStations"
            android:label="trains" />
        <activity android:name=".trainrelated.DaysDisplayPost" />
        <activity android:name=".StartingActivities">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".trainrelated.TrainArrivalTime" />
        <activity android:name=".trainrelated.CancelledTrains" />
        <activity android:name=".trainrelated.RescheduledTrains"></activity>
    </application>

</manifest>